apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: codify

resources:
  - ../../base
  - server-config.yaml
  - gateway-networkpolicy.yaml
  - sandbox-rbac.yaml
  - sandbox-clusterrole.yaml
  - sandbox-clusterrolebinding.yaml

# Use the server image variant that includes the openshell SDK extra
# (built by CI with CODIFY_EXTRAS=openshell).
images:
  - name: ghcr.io/codify-ai/codify-server
    newName: ghcr.io/codify-ai/codify-server-openshell

patches:
  - path: configmap-patch.yaml
  - path: secret-patch.yaml
  - path: deployment-patch.yaml
