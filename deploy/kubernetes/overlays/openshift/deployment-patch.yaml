apiVersion: apps/v1
kind: Deployment
metadata:
  name: codify
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: codify
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
