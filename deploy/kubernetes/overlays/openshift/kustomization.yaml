apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: codify

resources:
  - ../../base
  - route.yaml

patches:
  - path: deployment-patch.yaml
  - target:
      kind: Ingress
      name: codify
    patch: |
      $patch: delete
      apiVersion: networking.k8s.io/v1
      kind: Ingress
      metadata:
        name: codify
