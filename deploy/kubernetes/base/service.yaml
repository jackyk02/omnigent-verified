apiVersion: v1
kind: Service
metadata:
  name: codify
  labels:
    app: codify
spec:
  type: ClusterIP
  selector:
    app: codify
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
