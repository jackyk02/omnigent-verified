"""API schemas (protobuf) for codify services.

Each API lives in its own subpackage, versioned to mirror its proto ``package``
declaration — e.g. the routing API's ``package codify.api.routing.v1`` maps to
``codify/api/routing/v1/``. A new major version is a sibling ``vN`` package.
"""
