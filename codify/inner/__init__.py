"""Pre-integration codify code. Moved here during unification.

Internal imports between files here use relative syntax (from .X import
Y). Cross-boundary imports from codify-native code inside
codify/ use the explicit path (codify.inner.X) so the
dependency is grep-findable.
"""
